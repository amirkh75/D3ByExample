Django==3.2.13
gunicorn==20.1.0
psycopg2-binary==2.9.1
Pillow==7.0.0
social-auth-app-django==3.1.0
django-extensions==2.2.5
werkzeug==0.16.0
certifi
# In order to use the urllib to retrieve images from URLs served through HTTPS,
# you need to install the Certifi Python package. Certifi is a collection of root
# certificates for validating the trustworthiness of SSL/TLS certificates.

easy-thumbnails==2.7